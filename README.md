# 🖼️ SVG → PNG (Client-Side Converter)

A tiny single-page tool to convert **SVG** files to **PNG** images directly in your browser.  
No server, no upload — everything happens **locally** (Canvas + DOM).
